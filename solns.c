/* Enter your solutions in this file */
#include <stdio.h>
float average(int arr[],int len){
  int sum = 0;
  float avg;
  for(int i = 0; i < len; i++){
    sum += arr[];
  }
  avg = (float)sum/len;
  return avg;
}
  
